if(window.location.pathname === "/") require('./home');
if(window.location.pathname === "/transactions") require('./transactionHistory');
if(window.location.pathname === "/manage") require('./manage');
